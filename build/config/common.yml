default:
  app:
    name: Hello world application
    version: 0.1.0

  broker:
    mqtt:
      url: mqtt://test.mosquitto.org
      topics: ["hello"]
      qos:
      protocol: mqtt
      retain:
      subscribe: true

development:

test:

staging:

production:
